# 🚀 Инструкция по деплою DataHub Backend на Render

## ✅ Быстрый деплой (рекомендуется)

### Шаг 1: Создать аккаунт на Render
1. Перейти на [render.com](https://render.com)
2. Нажать "Get Started" или "Sign Up"
3. Войти через GitHub аккаунт

### Шаг 2: Подключить репозиторий
1. В Render Dashboard нажать "New +" → "Web Service"
2. Выбрать "Build and deploy from a Git repository"
3. Нажать "Connect" рядом с `MusekiKuro/uniguidebackend`

### Шаг 3: Настройка сервиса
Заполните следующие поля:

- **Name**: `uniguide-backend` (или любое другое имя)
- **Region**: `Frankfurt (EU Central)` (ближе к Казахстану)
- **Branch**: `master`
- **Runtime**: `Python 3`
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`
- **Instance Type**: `Free`

### Шаг 4: Добавить переменные окружения
В разделе "Environment Variables" добавьте:

```
PYTHON_VERSION=3.13.0
ANTHROPIC_API_KEY=ваш_ключ_anthropic
GEMINI_API_KEY=ваш_ключ_gemini
```

**Важно:** Если у вас есть только один API ключ (например, Gemini), уберите другой.

### Шаг 5: Запустить деплой
1. Нажать "Create Web Service"
2. Подождать 3-5 минут пока приложение запустится
3. После успешного деплоя вы получите URL:
   ```
   https://uniguide-backend.onrender.com
   ```

### Шаг 6: Проверить работу
Откройте в браузере:
```
https://uniguide-backend.onrender.com/api/health
```
Должен вернуть: `{"status":"ok"}`

---

## 🔧 Альтернативный метод: Railway

### Быстрый деплой через Railway

1. Перейти на [railway.app](https://railway.app)
2. Войти через GitHub
3. Нажать "New Project" → "Deploy from GitHub repo"
4. Выбрать `MusekiKuro/uniguidebackend`
5. Railway автоматически определит Python и начнет деплой
6. Добавить переменные окружения в Settings → Variables

---

## 🔄 Обновление Frontend

После успешного деплоя backend обновите `API_BASE_URL` во frontend:

### В index.html:
```javascript
// Замените:
const API_BASE_URL = 'http://127.0.0.1:8000/api';

// На:
const API_BASE_URL = 'https://uniguide-backend.onrender.com/api';
```

### В result.html:
```javascript
// То же самое
const API_BASE_URL = 'https://uniguide-backend.onrender.com/api';
```

---

## ⚠️ Важные моменты

### 1. Free tier ограничения Render:
- Приложение "засыпает" после 15 минут бездействия
- Первый запрос после "пробуждения" может занять 30-60 секунд
- 750 часов бесплатного использования в месяц

### 2. CORS:
Если возникнут ошибки CORS, проверьте настройки в `app/main.py`:
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # В продакшене указать конкретные домены
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### 3. Логи:
Чтобы посмотреть логи на Render:
1. Открыть Dashboard
2. Выбрать ваш сервис
3. Перейти во вкладку "Logs"

---

## 📝 Checklist для Чекпоинта 3

- [ ] Backend задеплоен на Render
- [ ] API отвечает на `/api/health`
- [ ] Обновлен API_BASE_URL во frontend
- [ ] Проверен поиск через frontend
- [ ] Ссылка на рабочее демо добавлена в опрос

---

## 🎉 Готово!

После деплоя ваш API будет доступен по адресу:
```
https://your-app-name.onrender.com/api
```

Используйте этот URL в frontend и в опросе на сайте хакатона!
